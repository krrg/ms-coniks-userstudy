version: '3.1'
services:

  db:
    image: mariadb
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: thesis
      MYSQL_USER: brad
      MYSQL_PASSWORD: password
    ports:
      - 127.0.0.1:3306:3306
